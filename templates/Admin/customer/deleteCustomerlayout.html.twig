<div class="col-12">
<div class="card">
        <h5 class="card-header">Delete Account</h5>
        <div class="card-body">
            <div class="mb-3 col-12 mb-0">
            <div class="alert alert-warning">
                <h6 class="alert-heading fw-bold mb-1">Are you sure you want to delete this customer account?</h6>
            </div>
            </div>
            {{ form_start(formDelete) }}
                {{ form_row(formDelete.confirmation) }}
                <label class="form-check-label" for="accountActivation">I confirm account deactivation</label>
                <hr>
                {{ form_row(formDelete.Deactivation) }}
            {{ form_end(formDelete) }}
        </div>
        </div>
</div>