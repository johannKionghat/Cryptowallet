<div class="col-xl-12">
  <div class="nav-align-top mb-4">
    <ul class="nav nav-pills mb-3" role="tablist">
        <li class="nav-item">
        <a href="{{path('wallet')}}">
           <button
            type="button"
            class="nav-link"
            role="tab"
            data-bs-toggle="tab"
            data-bs-target="#navs-pills-top-Add"
            aria-controls="navs-pills-top-Add"
            aria-selected="false">
            All
        </button>
        </a>
        </li>
        <li class="nav-item">

        <button
            type="button"
            class="nav-link {{ app.current_route == 'wallet.add' ? 'active' : ''}}"
            role="tab"
            data-bs-toggle="tab"
            data-bs-target="#navs-pills-top-Add"
            aria-controls="navs-pills-top-Add"
            aria-selected="false"
        >
        New wallet
        </button>
        </li>
    </ul>
    {# content of tabs #}
    <div class="tab-content">
        <div class="tab-pane fade show" id="navs-pills-top-Customer" role="tabpanel">

        </div>
        
        <div class="tab-pane fade show active" id="navs-pills-top-Add" role="tabpanel">
        <!-- Content -->
            <div class="col-md-12">
                {% include 'partials/flashes.html.twig' %}
                <div class="card mb-4">
                <h5 class="card-header">Add new wallet</h5>
                <!-- Account -->
                <div class="card-body">
                    <div class="d-flex align-items-start align-items-sm-center gap-4">
                    {{ form_start(formWallet) }}
                    </div>
                </div>
                <hr class="my-0" />
                <div class="card-body">
                    <div class="row">
                        <div class="mb-3 col">
                        <label for="firstName" class="form-label">Name</label>
                        {{form_row(formWallet.Name)}}
                        </div>
                        <div class="d-flex mt-2 gap-2">
                            {{ form_row(formWallet.Save) }}
                            <a href="{{path('wallet')}}" class="btn btn-secondary">Annuler</a>
                        </div>
                        {{ form_end(formWallet) }}
                    </div>
                <!-- /Account -->
                </div>
            </div>
        </div>
    </div>
  </div>
</div>


<!-- / Content -->