<div class="col-12">
<div class="card">
        <h5 class="card-header">Delete Wallet</h5>
        <div class="card-body">
            <div class="mb-3 col-12 mb-0">
            <div class="alert alert-warning">
                <h6 class="alert-heading fw-bold mb-1">Are you sure you want to delete this wallet ? if you delete this wallet it will not be possible for you to retore your crypto, make sure you have balance is empty ! </h6>
            </div>
            </div>
            {{ form_start(formDeleteWallet) }}
                {{ form_row(formDeleteWallet.confirmation) }}
                <label class="form-check-label" for="accountActivation">I confirm wallet deactivation</label>
                <hr>
                <div class="d-flex gap-2">
                    {{ form_row(formDeleteWallet.Delete) }}
                    <a href="{{path('wallet')}}" class="btn btn-secondary">Annuler</a>
                </div>
            {{ form_end(formDeleteWallet) }}
        </div>
        </div>
</div>